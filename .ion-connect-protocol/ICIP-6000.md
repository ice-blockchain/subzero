ICIP-6000
======

Request Blockchain Assets and Notify of sending them
------------------------------------

`draft` `optional`

The goal of this ICIP is to facilitate the transferring of various blockchain assets/funds between 2 users.

# Receive funds/assets

When an user wants to receive an asset from another user, the client MUST send a kind `1755` event to the user that's being requested the asset's relay.

The `p` and `l` + `L` tags are mutual exclusive: you can either have a `p` tag or a `l`+`L` tag pair, but never both.
The `L` tag MUST be set to `wallet.address` and the `l` tag MUST be the address from `content.from`.

The `network`, `asset_class` and (`p` or `l`+`L`) tags MUST be present in every `1755` event. The `asset_address` is optional for native coin transfers but mandatory for all the rest.

The event SHOULD be a [rumor (an event that is not signed) then sealed and wrapped](https://github.com/nostr-protocol/nips/blob/master/59.md) to hide the user's blockchain activity on the platform.

```jsonc
{
  "kind": 1755,
  "content": {
                "amount": <the transfer amount in native discriminator:I.E. if bitcoin, amount is in Satoshi; used only for coins/fungible tokens >,
                "amount_usd": <the decimal value amount priced in USD at the time of the transfer; used only for coins/fungible tokens>,
                "asset_id": <the identificator of the NFT in the collection; used only for NFTs>,
                "from" : <address>,
                "to": <address>
             },
  "tags": [
    ["network", <network name>],
    ["asset_class", <class name>],
    ["asset_address", <address or any other relevant identifier>],
    --------
    ["p", <pubkey of the "from" address>],
    -- OR --
    ["L", "wallet.address"],
    ["l", < <content.from> address>, "wallet.address"],
    --------
    ...
  ],
  // other fields...
}
```

# Notify sending of funds/assets 

After the user transfers the asset/funds on the blockchain, a kind `1756` event must be sent to that user's relay. Additionally, if the transfer was triggered by an initial receive funds/assets request, the event MUST have the `request` tag set and that event must also be sent to the user that requested the transfer's relay as well.

The `p` and `l` + `L` tags are mutual exclusive: you can either have a `p` tag or a `l`+`L` tag pair, but never both.
The `L` tag MUST be set to `wallet.address` and the `l` tag MUST be the address from `content.to`.

The `network`, `asset_class` and (`p` or `l`+`L`) tags MUST be present in every `1756` event. The `asset_address` is optional for native coin transfers but mandatory for all the rest.

The event SHOULD be a [rumor (an event that is not signed) then sealed and wrapped](https://github.com/nostr-protocol/nips/blob/master/59.md) to hide the user's blockchain activity on the platform. 

```jsonc
{
  "kind": 1756,
  "content": {
                "amount": <the transfer amount in native discriminator:I.E. if bitcoin, amount is in Satoshi; used only for coins/fungible tokens >,
                "amount_usd": <the decimal value amount priced in USD at the time of the transfer; used only for coins/fungible tokens>,
                "tx_hash": <the hash of the transaction>,
                "asset_id": <the identificator of the NFT in the collection; used only for NFTs>,
                "tx_url": <the url of the transaction from an explorer>,
                "from" : <address>, 
                "to": <address>
             },
  "tags": [
    ["network", <network name>],
    ["asset_class", <class name>],
    ["asset_address", <address or any other relevant identifier>],
    --------
    ["p", <pubkey of the "to" address>],
    -- OR --
    ["L", "wallet.address"],
    ["l", < <content.to> address>, "wallet.address"],
    --------
    ["request", <the JSON stringified version of the 1755 receive request event, if any>],
    ...
  ],
  // other fields...
}
```

### Referencing `1755` & `1756` into E2EE chat messages

Whenever clients need to reference a sent transaction event `(1756)` or a request sending of a transaction event `(1755)` into a kind `14` event, they MUST do the following:
1. add a [`nevent` bech32-encoded entity](https://github.com/nostr-protocol/nips/blob/master/19.md) with 3 TLV entries (event id, event pubkey/master key-preferred-, event kind) in the event `content` that points to the `1755` or `1756` event
2. add an `e` tag with the event id pointing to the `1755` or `1756` event 

###### Example 
```json
{
  "kind": 14,
  "tags": [
     ["h", "<UUIDV7>"],
     ["p", "<receiver-1-pubkey>", "<relay-url>"],
     ["p", "<receiver-2-pubkey>", "<relay-url>"],
     ["subject", "<conversation-title>"],
     ["e", "<kind-14-id>", "<relay-url>", "root/reply"], // if this is a reply
     ["e", "<kind-1755/1756-id>", "<relay-url>"] 
    // rest of tags...
  ],
  "content": "Hello Joe, can you please lend me some coins by end of month? nostr:nevent1qqsqt0gcggry60n72uglhuhypdlmr2dm6swjj69jex5v530gcpazlzszyqalp33lewf5vdq847t6te0wvnags0gs0mu72kz8938tn24wlfze6qcyqqqqdhql7azf4",
  // rest of fields ...
}
```

### Client & Relay Considerations

Both clients and relays MUST validate the events accordingly.

Relays MUST not index the `request` tag in any way.